# Templates Skill

Coleção de templates práticos para n8n. Cada template inclui: propósito, camadas, pré-requisitos e checklist de segurança.

Local dos exemplos: `examples/` (JSON exportável). Importe no n8n via *Import from File*.

Para automação/integração, use `C:\Users\santo\.agent\scripts\n8n_tools.py` e `deploy_templates.ps1`.
