# Uso — Templates

1. Importe arquivos JSON em `examples/` via *Import from File* no n8n.
2. Atualize `Credentials` locais após importar (não deixe chaves embutidas).
3. Valide fluxo em `staging` antes de ativar em `production`.
