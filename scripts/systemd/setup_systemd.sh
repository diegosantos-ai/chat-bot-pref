#!/bin/bash

# Legacy: nao instalar unidades systemd a partir deste repositorio nesta fase.

echo "Este diretório e legado e nao deve ser usado para instalar services systemd."
echo "Use Docker Compose para execucao local."
echo "O fluxo de deploy alvo sera redefinido com GitHub Actions + Terraform/AWS."
exit 1
